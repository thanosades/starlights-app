import { makeStyles } from '@mui/styles';

export default makeStyles((theme) => ({
  paper: {
    padding: '3%',
  },
  form: {
    display: 'flex',
    height: '420px',
    flexWrap: 'wrap',
    justifyContent: 'center',
    alignItems: 'baseline'
  },
  fileInput: {
    width: '97%'
  }
}));
